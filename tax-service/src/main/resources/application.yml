server:
  port: 8091

spring:
  application:
    name: tax-service

  datasource:
    # IMPORTANT: request these env vars from the orchestrator/user as needed
    url: ${TAX_DB_URL:jdbc:postgresql://localhost:5432/shopizer}
    username: ${TAX_DB_USER:shopizer}
    password: ${TAX_DB_PASSWORD:shopizer}
    driver-class-name: org.postgresql.Driver

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: shopizer

  flyway:
    enabled: true
    # Tax service owns tax tables in the shared "shopizer" schema for now.
    default-schema: shopizer
    schemas: shopizer
    locations: classpath:db/migration

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8080/realms/shopizer}

management:
  endpoints:
    web:
      exposure:
        include: health,info
